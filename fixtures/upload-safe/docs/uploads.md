# Upload boundary

Uploads use a bounded content length, a validated MIME type, and a tenant-scoped storage key. Authorization is enforced by the route's authenticated caller in the deployment adapter.
