# Remediation contract

`prodos fix <CONTROL_ID>` is plan-first. The current engine can apply one preconditioned low-risk Next.js security-header change with `--apply`; other controls remain explicit plans. Every remediation exposes preconditions, risk classification, affected files, implementation strategy, tests, verification procedure, and rollback guidance. Architecture-sensitive or destructive changes remain agent- or human-reviewed.
